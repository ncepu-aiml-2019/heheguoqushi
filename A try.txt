%连续信号由三个频率信号1900Hz\2100Hz\3000Hz组成。

clc;close all;clear all;
N1=？;N2=？ L1=？; L2=？%时域取样点数N、频域取样点数L1，L2，自行实验

n=0:N-1;

f1=1900;f2=2100;f3=3000;%三个频率分量

fs=？;%采样频率，自行设计

x1=sin(2*pi*f1/fs*N1)+5*sin(2*pi*f2/fs*N1)+3*sin(2*pi*f3/fs*N1);%时域采样后的序列,长度为N1
x2=? %时域采样后的序列,长度为N2

%对于x1的频谱分别进行L1,L2点DFT运算
X11=fft(x1,L1); 
X12=?;

%对于x2的频谱分别进行L1,L2点DFT运算
X21=fft(x2,L1);
X22=？

%以下，画出频谱图，注意是幅度谱
stem(abs(X11))